import { PublicKey } from "@solana/web3.js";

// Default wallet address
export const DEFAULT_WALLET_ADDRESS: PublicKey = new PublicKey(
  "WALLET_ADDRESS_PLACEHOLDER" // Replace with your actual default wallet address
);
